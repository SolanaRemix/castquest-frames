{
  "name": "@castquest/strategy-worker",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "[ -d src ] && tsc -p tsconfig.json || echo 'Skipping build - no src directory'",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "run:once": "ts-node src/index.ts --once"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
